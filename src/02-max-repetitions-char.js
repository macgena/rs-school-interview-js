/**
 * Given a string.
 * Return a character that repeats more than others.
 * If there are more than one character with the same repetitions count,
 * return first in alphabetical order.
 *
 * If string is empty return null.
 *
 * @param {String} string
 * @return {String}
 *
 * @example
 * string = 'aabcaawe'
 *
 * The result should be 'a'
 *
 * @example
 * string = 'aabb'
 *
 * The result should be 'a'
 */
 function maxRepetitionsChar(/* string */) {
  throw new Error('Not implemented');
}

module.exports = maxRepetitionsChar;
