/**
 * Given a string.
 * Return a string with inverted characters.
 *
 * @param {String} string
 * @return {String}
 *
 * @example
 * string = 'Hello world!'
 *
 * The result should be '!dlrow olleH'
 */
 function reverseString(/* string */) {
  throw new Error('Not implemented');
}

module.exports = reverseString;
